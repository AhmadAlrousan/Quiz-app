
import 'package:flutter/material.dart';

class Result extends StatelessWidget {
final Function restart;
const Result(this.restart);
  @override
  Widget build(BuildContext context) {
    return Center();
  }
}
